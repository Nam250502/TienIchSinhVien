@model TienIchSinhVien.Models.ProfileViewModel
@{
    ViewBag.Title = "detaiprofile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row py-5 px-4">
    <div class="col-md-12 mx-auto">
        <!-- Profile widget -->
        <div class="bg-white shadow rounded overflow-hidden">
            <div class="px-4 pt-0 pb-4 cover">
                <div class="media align-items-end profile-head">
                    <div class="profile mr-3">
                        <img src="@Model.Anh"
                             alt="..." width="130" class="rounded mb-2 img-thumbnail"><a href="#"class="btn btn-outline-dark btn-sm btn-block">@Model.Name</a>
                    </div>
                    @*<div class="media-body mb-5 text-white">
                        <h4 class="mt-0 mb-0">Mark Williams</h4>
                        <p class="small mb-4"> <i class="fas fa-map-marker-alt mr-2"></i>New York</p>
                    </div>*@
                </div>
            </div>
 
            <div class="px-4 py-3">
                <h5 class="mb-0">Chi Tiết Đánh Giá</h5>
                <div class="p-4 rounded shadow-sm bg-light">
                    @Html.Action("loadDanhGia", "Profile")
                </div>
            </div>
            </div>
        </div>
    </div>